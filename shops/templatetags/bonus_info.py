# from django import template
#
# register = template.Library()


    #return value.replace(arg,"")
# в параметрах filter строка 'cut' = так будет называться самопальный фильтр в, а НЕ строка  cut =  название функции
#register.filter('cut',cut)
# @register.inclusion_tag('_extra/nav.html')
# def bonus_info(user):
#     bonus_id= user.bonus.last()
#     return {'bonus_id': bonus_id}
